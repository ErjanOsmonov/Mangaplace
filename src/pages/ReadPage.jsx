import React from "react";

// In development, will finish when done with backend, to not store every single manga chapter photo in folder

const ReadPage = () => {
    return (
        <div>
            
        </div>
    )
}

export default ReadPage;